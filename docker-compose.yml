version: "3.8"
services:
  netwatch:
    image: ghcr.io/YOURUSERNAME/netwatch-dashboard:latest
    container_name: netwatch-dashboard
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./data:/app/data